import { Router } from "express";

// Создаём экземпляр роутера Express.
// Каждый роутер = отдельная группа API-эндпоинтов (маршрутов).
const router = Router();

/**
 * GET /
 * Этот маршрут нужен как "health-check" — чтобы убедиться,
 * что backend запущен и отвечает на запросы.
 *
 * Можно открыть в браузере:
 * 👉 http://localhost:4000/
 */
router.get("/", (req, res) => {
  res.json({ message: "Backend is running ✅" });
});

export default router;
